name: CI

on:
  pull_request:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  test:
    name: Node ${{ matrix.node }} / ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    timeout-minutes: 35
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-latest
            node: 18.18.0
          - os: ubuntu-latest
            node: 22.x
          - os: ubuntu-latest
            node: 24.x
          - os: macos-latest
            node: 22.x
          - os: macos-latest
            node: 24.x
          - os: windows-latest
            node: 22.x
          # Node 24.16+ has a confirmed libuv fs.watch abort on Windows Server 2025:
          # https://github.com/nodejs/node/issues/63638
          # Keep the Windows Node 24 baseline on the last unaffected release.
          - os: windows-latest
            node: 24.15.0

    steps:
      - name: Check out repository
        uses: actions/checkout@v7

      - name: Set up Node.js
        uses: actions/setup-node@v6
        with:
          node-version: ${{ matrix.node }}
          cache: npm

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: npm ci

      - name: Install image-processing test dependencies
        run: python -m pip install --disable-pip-version-check --no-input Pillow numpy

      - name: Run tests
        run: npm test

      - name: Validate plugin manifest and Skill contracts
        run: npm run validate:contracts

      - name: Run visual smoke checks
        if: startsWith(matrix.node, '24.')
        run: npm run smoke:visual

      - name: Run Windows visual regression checks
        if: runner.os == 'Windows' && startsWith(matrix.node, '24.')
        run: npm run visual:regression

      - name: Verify release metadata
        if: startsWith(matrix.node, '24.')
        run: npm run verify:release

      - name: Verify release package
        if: startsWith(matrix.node, '24.')
        run: node scripts/build-release.mjs --output-dir dist/release-ci

      - name: Smoke test installed release package
        if: startsWith(matrix.node, '24.')
        run: node scripts/verify-release-archive.mjs dist/release-ci
